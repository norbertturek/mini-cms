services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mini_cms}
      POSTGRES_USER: ${POSTGRES_USER:-mini_cms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mini_cms}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mini_cms} -d ${POSTGRES_DB:-mini_cms}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
